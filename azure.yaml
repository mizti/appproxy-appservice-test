# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: appproxy-appservice-test
metadata:
  template: appproxy-appservice-test@0.0.1
services:
  web:
    project: ./app
    language: python
    host: appservice
